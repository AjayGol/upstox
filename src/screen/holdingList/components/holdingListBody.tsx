import React from 'react';
import {IHoldingListProps} from '../holdingList.types';
import {View} from 'react-native';

const HoldingListBody: React.FC<IHoldingListProps> = ({}) => {
  return <View />;
};

export default HoldingListBody;
